//#region src/index.ts
/** Host loader entry for the browser-only extension-center plugin. */
/** Provides no host-side behavior; the browser half talks to the desktop IPC bridge. */
function apply() {}
//#endregion
export { apply };
