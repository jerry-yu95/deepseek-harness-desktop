# 聚合包清单：patchFrom 的包其 cordis.patch.yml insert 行会被汇总进本包 patch
# deps 的包进 package.json dependencies（workspace:*）
# 自定义图片皮肤保留官方布局，不再聚合预置皮肤包。
patchFrom:
  - ../skins/skin-center
deps:
  - ../skins/skin-center
