# JIWEI 0.1.45 发布验收报告

## 状态

- 更新日期：2026-09-14
- 当前阶段：知识回归 Task 0–12 和工程收口 Task 1–5 已完成本地验收；发布门禁仍有待验项
- 源码版本：`0.1.44`；`0.1.45` 为未发布目标
- 起始分支：`main`
- 执行分支：`codex/jiwei-0.1.45-readiness`
- 执行起点：`94f10af`
- 原 `0.1.44` 代码基线：`4cdd16f`
- 回滚版本：`desktop-v0.1.44`
- 发布状态：未发布
- 当前核查基点：HEAD `f4fd631` 加未提交工作区；不能仅凭该 SHA 重建当前修复

本报告是当前状态入口。[审阅地图](2026-09-12-review-map.md) 说明改动分组和共享文件，[验收矩阵](0.1.45-acceptance-matrix.md) 保留分层结果，[知识回归记录](../plans/2026-09-08-knowledge-regression-progress.md) 保留 Task 0–12 的历史失败与修复证据。不同计划中的 Task 编号不代表同一发布阶段。

## 2026-09-14 最新交付补充

用户已授权 DMG 打包及 GitHub 源码同步。当前代码已兼容官方 `0.1.5-rc.2`，补齐安装包遗漏的三个 peer 运行依赖；最终 Apple Silicon DMG 镜像、签名及 263 个运行包检查通过，镜像内知识 9、通用流程 5、官方附件 3 个合成场景全部通过。文件、校验值和边界见 [打包及同步报告](2026-09-14-dmg-and-github-sync.md)。本次为开发分支源码同步及本地包交付，应用版本仍为 0.1.44，不代表 0.1.45 正式发布。

下述 `HEAD f4fd631`、未提交状态和旧版门禁为相应日期的历史快照；本轮源码以新增报告所属提交为准。完整工作区构建、类型及 1,132 项包测试已在官方兼容轮通过，详见 [兼容验收记录](2026-09-13-harness-compatibility.md)。

## 2026-09-13 最新源码补充

用户验证当日 DMG 后的第二轮修复已完成：摘要预算与推理档位、视频占位、分块摘要编辑、三个入口定位以及草稿保护。最新结果为知识 92/92、扩展中心 105/105、桌面 257/257，两包类型与构建通过，Electron 阅读 7/7、通用验收 5/5。随后按用户授权生成 `dmg-20260913-r2-JXbsa2/JIWEI-0.1.44-20260913-r2-arm64.dmg`，镜像校验、签名、218 个运行包及镜像内 12 个合成场景通过，详见 [二轮报告](2026-09-13-reading-workflow-repair.md)。下述不带 r2 的历史 DMG 不包含第二轮改动；真实模型与用户文章尚未验证。

以下为第二轮之前的结果：

公众号图片网络兼容、摘要结构、编辑器和知识卡片排版已完成本地合成验收，详见 [本轮报告](2026-09-13-article-reading-polish.md)：知识 88/88、扩展中心 95/95、桌面 256/256，两包类型与构建通过，阅读 Electron 7/7、通用验收 5/5、真实 Chromium 本地代理测试 2 项通过。真实用户文章与模型尚未验证。随后用户授权生成 2026-09-13 新 DMG，详情见文末；2026-09-12 安装包仍是旧快照。

## 2026-09-12 本地结果快照

2026-09-12，macOS arm64、Node `22.22.3`、pnpm `11.21.0`，当前未提交源码工作区。CI 配置为 Node 24，本地环境不完全等同 hosted CI。使用既有依赖、合成文章/模型/MCP 服务和独立临时配置；没有操作真实账号。先构建两个依赖包，再执行桌面验证。

| Check | Command | Result |
| --- | --- | --- |
| Knowledge tests | `pnpm --filter @harness-design/dsh-knowledge test` | PASS，85/85 |
| Extension Center tests | `pnpm --filter @linxin666/dsh-client-ui-extension-center test` | PASS，93/93；本地设置 `DEBUG_PRINT_LIMIT=0` |
| Desktop tests | `pnpm --filter @harness-design/desktop test` | PASS，249/249，含新增 3 项证据辅助测试 |
| Knowledge typecheck/build | 对知识包分别执行 `typecheck`、`build` | PASS |
| Extension Center typecheck/build | 对扩展中心分别执行 `typecheck`、`build` | PASS |
| Knowledge Electron | `pnpm --filter @harness-design/desktop test:knowledge:e2e` | PASS，7/7 |
| Composer Electron | `pnpm --filter @harness-design/desktop test:composer:e2e`，zh/en 分别执行 | PASS，各 15/15；宽布局中心偏差均为 0px |
| General Electron acceptance | `pnpm --filter @harness-design/desktop test:acceptance` | PASS，5/5；主动重载产生 25 次预期传输断开 |
| CI syntax / diff | YAML 解析、依赖顺序检查、`git diff --check` | PASS（本地静态检查） |

这组结果覆盖三个受影响包，不是全 monorepo。构建仍有既有 tsdown 配置弃用和模块类型提示，无构建错误。未在本轮重新安装依赖，冻结锁文件安装及干净 checkout 的可复现性仍需 hosted CI 验证。

## 历史基线结果（保留，非当前验收）

| Check | Command | Result |
| --- | --- | --- |
| Desktop tests | `pnpm desktop:test` | PASS — 210/210 |
| Extension Center tests | `pnpm --filter @linxin666/dsh-client-ui-extension-center test` | PASS — 46/46 |
| Knowledge tests | `pnpm --filter @harness-design/dsh-knowledge test` | PASS — 28/28 |
| Text context tests | `pnpm --filter @linxin666/dsh-text-context test` | PASS — 85/85 |

## 环境记录

首次在默认沙箱中执行时，桌面测试的 6 个 OAuth/运行时用例无法监听本机回环端口，三个 Vitest 包在启动阶段无法解析 `localhost`。获得本机测试权限后，使用同一代码和同一命令重跑全部通过。该记录属于测试环境诊断，不是产品失败。

## 发布门禁

| Gate | Status | Evidence |
| --- | --- | --- |
| Public-surface audit | LOCAL_PASS | `pnpm audit:public` 通过；另对本轮四份计划/QA 文档显式应用相同规则，无发现；不是完整安全审计 |
| Affected-package tests/typecheck/build | LOCAL_PASS | 上表，三个受影响包；两包 typecheck/build |
| Full monorepo checks | NOT_RUN | 本轮没有覆盖其余包；旧 text-context 85/85 仅为历史结果 |
| Hosted Desktop CI | NOT_RUN | 已配置三平台单元门禁及 macOS arm64 Electron，尚未提交运行 |
| Attachment/MCP isolated acceptance | LOCAL_PASS | 本轮 5/5，合成 MCP/附件/编辑重载；不证明真实业务操作 |
| My Brain / article images / composer | LOCAL_PASS | 本轮知识 7、双语 composer 各 15，全部新证据汇总为 passed |
| TAPD live read | NOT_RUN | 需真实服务授权及账号操作；凭证只在应用内输入 |
| iWiki live read | NOT_RUN | 需真实服务授权、网络与权限 |
| Real model / WeChat network | NOT_RUN | 合成摘要错误和微信文章 fixtures 不代表真实服务验证 |
| macOS arm64 local DMG | LOCAL_PASS | 2026-09-12 按用户要求打包当前 0.1.44 修复快照；镜像、签名、运行依赖及只读镜像内 12 个合成场景通过，详见下节 |
| Other installers / update / rollback | NOT_RUN | Windows、macOS Intel、安装覆盖、更新及回滚尚未验收；本地 DMG 未发布 |

`LOCAL_PASS` 表示当前本机检查通过，不能写成 CI、安装包或真实账号通过。`NOT_RUN` 表示未执行，不暗示已发现外部故障。

## 证据留存与已知边界

三个 Electron 脚本读取 `DSH_E2E_ARTIFACTS_ROOT`，每次创建唯一子目录并输出位置。保留合成截图、汇总 `result.json` 和 composer 几何数据；临时 profile 单独存放并清理。CI 无论成功失败均尝试上传这些 PNG/JSON，保留七天；不上传配置、日志或真实内容。测量模式 `DSH_COMPOSER_PROBE=1` 标为 `probe`，不能作为通过结果。

本轮证据目录标识：`jiwei-knowledge-reading-1hVlHy`、`jiwei-composer-zh-B1G03D`、`jiwei-composer-en-FibZ9z`、`jiwei-acceptance-z2Q8OF`。共 46 张 PNG 和 6 份 JSON；已核对四份结果计数、双语几何数据及文件类型，并打开检查知识导入、英文窄屏流式输入、连接器重载后的截图。完整本地定位见工程收口计划，临时文件可能被系统清理。

标准 OAuth 当前需要动态客户端注册，不包含自动刷新、预注册客户端及专有 SSO。图片以匿名方式抓取，依赖登录/Cookie 的图片可能只显示占位。真实模型原始故障的成因尚未由真实账号复现；合成兼容性修复不构成该结论。Windows/Intel 的真实 Electron 界面亦未在本轮本地验证。

工程收口轮没有提交、推送、版本变更、打包或发布。随后用户单独授权 DMG 打包，结果如下；后续发布决策仍需其余门禁的实际证据。

## 2026-09-12 用户授权的本地 DMG

- 文件：`apps/dsh-desktop/dist/dmg-20260912-cTId71/JIWEI-0.1.44-20260912-arm64.dmg`，208,436,170 bytes（约 199 MiB）。独立输出目录保留旧包，源码版本仍为 0.1.44，包含截至 2026-09-12 打包时的未提交修复，不包含后续变更。
- SHA-256：`6730fbc47bcf33deb9e46bfa62f2ae02007ec38c352394127d5c8117322eb38b`；同目录 `SHA256SUMS.txt` 已复核。
- `hdiutil verify`、`codesign --verify --deep --strict`、运行依赖校验通过；确认 218 个运行包和 arm64 原生依赖可用。九个关键源码/生成文件与工作区字节一致。
- 直接从只读挂载 DMG 的应用执行隔离知识阅读 7/7、通用附件/知识/MCP 验收 5/5。使用 `DSH_DESKTOP_E2E_EXECUTABLE` 指向镜像内程序、独立临时 profile 和合成数据；主动重载的 45 次预期传输断开单独计数。验证后卸载临时镜像。
- 证据标识：`jiwei-dmg-evidence-YNQYyl` 下的 `jiwei-knowledge-reading-ZYNW22`、`jiwei-acceptance-oMGGDu`；知识图片缓存截图已打开检查。未覆盖现有安装或真实账号资料。
- 本包采用本地 ad-hoc 签名，未进行 Apple Developer ID 签名或公证；这是本地测试包，不构成 0.1.45 正式发布或跨平台验收通过。

## 2026-09-13 用户授权的更新 DMG

- 文件：`apps/dsh-desktop/dist/dmg-20260913-ewrmFO/JIWEI-0.1.44-20260913-arm64.dmg`，208,390,934 bytes（约 199 MiB）；保留旧包，未改版本号，未发布。
- SHA-256：`50bc67559e06e76b21fefadc4e7d473717f2745cc66432ba505f0054b84a2584`；同目录 `SHA256SUMS.txt` 已重新校验。
- 重新构建知识包与扩展中心后执行 arm64 DMG 打包。`hdiutil verify`、`codesign --verify --deep --strict` 和 218 个运行包及 arm64 原生依赖检查通过。包内三份桌面源码、六份知识包产物及扩展中心界面产物，共十个关键文件与当前工作区字节一致，包含本轮图片网络、摘要结构、编辑器和卡片排版修复。
- 直接启动只读挂载镜像内的 JIWEI，使用隔离配置与合成数据完成知识阅读 7/7、通用验收 5/5；主动重载的 38 次预期传输断开单独计数。摘要、编辑空间、标签操作和离线图片均覆盖；摘要截图已打开检查。验收后卸载临时镜像。
- 证据标识：`jiwei-dmg-evidence-20260913-97mw2j` 下的 `jiwei-knowledge-reading-wmjW7X` 和 `jiwei-acceptance-zOaz4z`。没有覆盖现有安装或操作真实账号。
- 本包仍为 Apple Silicon 本地 ad-hoc 签名测试包，未经 Apple 公证。历史失败图片需重新导入文章，新摘要结构需重新生成；真实文章与模型效果由后续用户验证。
